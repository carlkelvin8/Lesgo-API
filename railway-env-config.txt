# ============================================================
# COMPLETE RAILWAY ENVIRONMENT VARIABLES
# Copy these into Railway → Your Project → Variables tab
# ============================================================

# CRITICAL - Application Key (REQUIRED)
APP_KEY=base64:COiTe7awxy3GGN/AUPvxatGvteNg1NkOrwDa9HS9Ka4=

# Application Settings
APP_NAME=LeSGo
APP_ENV=production
APP_DEBUG=false
APP_URL=https://YOUR_RAILWAY_DOMAIN.up.railway.app

# Logging
LOG_CHANNEL=stderr
LOG_LEVEL=error

# CORS (Update with your actual domain)
CORS_ALLOWED_ORIGINS=https://YOUR_RAILWAY_DOMAIN.up.railway.app,http://localhost,http://localhost:8000,http://10.0.2.2:8000,http://10.0.2.2

# Database (Railway PostgreSQL Plugin - these should auto-populate)
DB_CONNECTION=pgsql
DB_HOST=${{Postgres.RAILWAY_PRIVATE_DOMAIN}}
DB_PORT=5432
DB_DATABASE=${{Postgres.POSTGRES_DB}}
DB_USERNAME=${{Postgres.POSTGRES_USER}}
DB_PASSWORD=${{Postgres.POSTGRES_PASSWORD}}

# Redis (Railway Redis Plugin - these should auto-populate)
REDIS_CLIENT=predis
REDIS_URL=${{Redis.REDIS_URL}}
REDIS_HOST=${{Redis.REDISHOST}}
REDIS_PORT=${{Redis.REDISPORT}}
REDIS_PASSWORD=${{Redis.REDISPASSWORD}}

# Cache/Queue/Session
CACHE_STORE=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis

# Reverb WebSockets
REVERB_APP_ID=lesgo-app
REVERB_APP_KEY=lesgo-reverb-key
REVERB_APP_SECRET=lesgo-reverb-secret
REVERB_HOST=0.0.0.0
REVERB_PORT=8080
REVERB_SCHEME=https

# Swagger Documentation
L5_SWAGGER_CONST_HOST=https://YOUR_RAILWAY_DOMAIN.up.railway.app

# Optional Services (Set these if you plan to use them)
FIREBASE_CREDENTIALS=storage/app/firebase-credentials.json
FIREBASE_PROJECT_ID=

TWILIO_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=

SEMAPHORE_API_KEY=
SEMAPHORE_SENDER_NAME=LESGO

XENDIT_SECRET_KEY=
XENDIT_PUBLIC_KEY=
XENDIT_WEBHOOK_TOKEN=
XENDIT_SUCCESS_URL=https://YOUR_RAILWAY_DOMAIN.up.railway.app/payment/success
XENDIT_FAILURE_URL=https://YOUR_RAILWAY_DOMAIN.up.railway.app/payment/failed

GCASH_WEBHOOK_SECRET=
MAYA_WEBHOOK_SECRET=